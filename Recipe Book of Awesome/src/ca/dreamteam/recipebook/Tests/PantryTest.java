//Mostly designed to be an example of JUnit test cases.
//There are lots of examples you can find by searching "JUnit 4 Eclipse Tutorial"
package ca.dreamteam.recipebook.Tests;

import static org.junit.Assert.*;

import org.junit.Test;

//import ca.dreamteam.recipebook.PantryActivity;
import ca.dreamteam.recipebook.Models.Ingredient;
/*
public class PantryTest
{    
    @Test
    public void test()
    {
        try
        {
            Ingredient i = new Ingredient();
            i.setName("Test Ingredient");
            i.setAmount(15.0f);
        
            PantryActivity p = new PantryActivity();
            p.createIngredient(i.toString());
            
            assertTrue(p.getAllIngredients().contains(i));
        }
        catch(Exception e)
        {
            fail(e.getMessage());
        }
    }

}
*/